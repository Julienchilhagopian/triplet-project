<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    {{{body}}}

      {{#if currentUser}}
      <p>Logged as mister {{currentUser.username}}
        <form method="POST" action="/auth/logout">
        <button>logout</button>
        </form>
      </p>
      {{/if}}
      
  </body>
</html>
