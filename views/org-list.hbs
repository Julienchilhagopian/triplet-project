<h1>Organisation list</h1>

<div>
    {{#each users}}
        <div> 
            <h2>{{this.username}}</h2>
            <p>{{this.description}}</p>
        </div>
        <button><a href="org/{{this._id}}">See more</a></button>
    {{/each}}
</div>
